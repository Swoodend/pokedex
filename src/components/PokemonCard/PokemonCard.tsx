import * as React from 'react';

interface PokemonCardProps {
};

// todo - this will become the main display component when we successfully fetch pokemon data (SW)
const PokemonCard = () => {

    return (
        <div>I am a pokemon card!</div>
    );
};

export default PokemonCard